package com.qaprosoft.shortesttrip.models.transport;

public class Tram extends CityTransport{

	@Override
	public String toString() {
		return "Tram [getNumber()=" + getNumber() + ", getId()=" + getId() + "]";
	}
}
